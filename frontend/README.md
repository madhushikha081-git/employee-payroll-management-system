# Frontend - HTML, Bootstrap, CSS and Browser Pages

`index.php` is the page layer. It renders the login page, Admin dashboard,
Employee dashboard, forms, tables, reports and responsive Bootstrap UI.

`../assets/style.css` contains the custom CSS for the project visual design.

For a live demo, show the login page, Admin Dashboard, Employees, Leave
Management, Payslips and Reports pages in the browser.
